# 自动排产系统-代码现状待办清单

> 更新日期：2026-03-25  
> 当前阶段：4.3 任务执行与生产化已完成，4.4 性能与覆盖正在持续收口。  
> 说明：项目事实以 docs/progress/自动排产系统-后端.md 与 docs/progress/自动排产系统-前端.md 为准，本清单只保留当前执行视角下的完成情况与下一步待办。

## 1. 当前已确认现状

- [x] 前端 cmd /c npm run lint 已于 2026-03-25 验证通过。
- [x] 前端 cmd /c npm test 已于 2026-03-25 验证通过。
- [x] 前端 cmd /c npm run build 已于 2026-03-25 验证通过。
- [x] 后端 pytest -q 已于 2026-03-25 验证通过，当前为 277 passed。
- [x] 正式认证链路已统一为 /api/auth/* + user_session。
- [x] 手动同步 / 定时同步已切到 PostgreSQL 队列 + 独立 worker / scheduler 进程。
- [x] 发图状态同步已收口为 detail_id + (order_no, material_no) 两类批量匹配。
- [x] BOM 补数候选已按 (material_no, plant) 在 SQL 侧去重，并按工厂口径判断现有 BOM。
- [x] Snapshot 刷新已复用预加载快照，避免批量 refresh 时按订单行再次逐条查现有 snapshot。
- [x] 停用角色分配、停用用户 session 失效、管理员角色切换权限回收等认证边界回归已补齐。

## 2. 已完成项

### 前端
- [x] 认证主链路已收口。
- [x] lint / test / build 工程链路已稳定通过。
- [x] 已拆分 src/views/AdminAccessPage.vue。
- [x] 已拆分 src/views/UserManagementPage.vue。
- [x] 已拆分 src/views/SyncConsolePage.vue。
- [x] 已拆分 src/views/BomDataPage.vue。
- [x] 已拆分 src/views/AssemblyTimeConfigPage.vue。
- [x] 列表查询 / 分页 / 排序复用能力已开始落地。
- [x] 表格空态 / 加载 / 错误反馈口径已收口。
- [x] Element Plus 按需加载与 ECharts chunk 告警已收口。

### 后端
- [x] 已完成 4.3：background_task / sync_scheduler_state、worker、scheduler、独立启动入口、Docker 服务拆分。
- [x] 已完成 4.4 首批热点：发图状态同步批量化、BOM 补数候选 SQL 去重、snapshot 刷新复用已加载快照。
- [x] 已完成 4.4 首批覆盖：停用角色分配限制、停用用户 session 立即失效、管理员角色切换权限即时回收。

## 3. 当前未完成待办

### P0
- [ ] 继续排查导出链路与剩余 BOM / snapshot 热点 SQL、批量组装与外部接口吞吐。
- [ ] 继续补禁用用户、空角色、角色切换、任务重试 / 恢复 / 调度开关等高风险回归。

### P1
- [ ] 为库存 / 产能实时校验预留统一读层抽象。
- [ ] 继续补 worker / scheduler 运维说明与 OpenAPI / 文档说明。
- [ ] 继续扩展 useServerTableQuery.ts、AppTableState.vue、useTableFeedbackState.ts 的复用范围。

### P2
- [ ] 继续拆分 PartCycleBaselinePage.vue、MachineCycleBaselinePage.vue 等高复杂度页面。
- [ ] 继续补前端关键页面回归测试。
- [ ] 继续清理 docs 中过期说明与历史遗留表述。

### P3
- [ ] 持续拆分后端超大服务文件，降低维护与回归风险。

### P4
- [ ] 在管理员角色稳定后，再扩展中文角色与细粒度 RBAC。

## 4. 接下来建议执行顺序

- [x] 优先完成 4.3 任务执行与生产化相关任务。
- [x] 收口正式认证 / 角色 / 用户主链路。
- [x] 收口 4.4 首批热点与首批高风险边界回归。
- [ ] 下一步优先继续处理导出链路 / 剩余 BOM / snapshot 热点。
- [ ] 之后继续补认证边界与任务恢复类回归测试。

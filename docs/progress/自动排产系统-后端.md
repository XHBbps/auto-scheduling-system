# 自动排产系统-后端

> 更新日期：2026-03-30
> 当前阶段：任务系统独立 worker / scheduler / 队列链路已落地，进入按整改清单并行推进性能、风险与工程化收口阶段

## 当前阶段总状态摘要

- 当前结论：P0 已收口；P1 中后台任务恢复测试、首批可观测性补强、worker / scheduler / snapshot 运维说明、BOM 回填候选加载热点、session 安全边界、OpenAPI 收口、RBAC 权限点骨架、权限矩阵/联动真源、统一读层以及 Dashboard 总览扩展聚合已落地；本轮又收口了 Docker 启动迁移竞争、worker claim/heartbeat stale recovery 误判问题，并补齐了自动同步调度器触发任务写入同步日志的链路，当前重点转向周期/时长字段业务边界的最后收口与剩余超大文件拆分。
- 当前最高优先级：`machine_cycle_days / machine_assembly_days / assembly_time_days / part_cycle_days / key_part_cycle_days` 业务边界最后收口、剩余超大文件拆分、发图/快照/导出链路隐性 N+1 持续巡检。
- 当前阻塞：暂无代码阻塞，文档中的乱码与占位符已清理。
- 下一步：继续把整机主周期、整机总装时长、部装装配时长、零件自身周期和关键件倒排周期的业务边界收成稳定语义，同时在 `schedule_snapshot_refresh_service.py` 剩余主流程壳层基础上继续做低风险拆分。
- 已完成关键能力：
  1. 新增正式用户、角色、用户角色关联和用户会话表，并提供 bootstrap 管理员账号。
  2. `/api/auth/login | /api/auth/session | /api/auth/logout` 已上线，返回正式用户会话信息。
  3. `common/auth.py` 不再回退 legacy `AdminSession`，正式鉴权只认 `UserSession`。
  4. 管理类、数据源类、导出类接口已按路由切到 `require_permission(...)` 权限点依赖，运行期不再锁死在单一 `admin` 角色判断。
  5. `/api/admin/users` 已提供用户列表、创建、启停用、重置密码、更新角色等能力。
  6. OpenAPI 描述与示例已同步为正式账号密码登录链路，并在本轮 Dashboard 总览扩展后重新导出 JSON 文档。
  7. Bootstrap 管理员初始化已要求显式配置 `BOOTSTRAP_ADMIN_PASSWORD`，不再回退旧令牌或弱默认密码。
  8. 新建用户时空角色不再自动补 `admin`，必须显式传入角色列表。
  9. 已补齐非 admin 登录成功但访问管理/数据源/导出接口返回 403 的权限边界测试，以及 OpenAPI 401/403 错误响应。
  10. 浏览器端已完成正式认证主链路回归，前后端联调下的登录、回跳、用户管理页刷新保活和退出后二次拦截均已实测通过。
  11. legacy 认证运行时代码链路已移除，legacy `admin_auth.py` / `admin_session.py` / `admin_session_repo.py` / `admin_auth_router.py` 已删除，正式会话 Cookie 配置与 OpenAPI 描述统一改为 `user_session` 口径。
  12. 历史联调文档已统一收口到 `/api/auth/*` + `user_session`，当前 API 联调说明不再把过时认证方式写成现行方案。
  13. 同账号再次登录会主动撤销旧会话，重置密码与停用用户时也会立即回收该用户全部活跃 session，session 安全边界已补齐。
  14. `app/main.py` 中的 OpenAPI 标签、说明、响应示例与增强逻辑已抽离到 `app/openapi.py`，应用启动装配层更聚焦，且未改动接口行为。
  15. `schedule_snapshot_refresh_service.py` 已拆出 `schedule_snapshot_refresh_helpers.py` 承接快照 payload 组装、warning/issue flags 计算与 stale 判断等纯辅助逻辑，排产快照主链路行为保持不变。
  16. `schedule_orchestrator.py` 已拆出 `schedule_orchestrator_helpers.py` 承接预校验失败响应、warning items、issue payload 与异常标记等纯辅助逻辑，排产编排主链路保持不变。
  17. 已新增 `background_task` 与 `sync_scheduler_state` 表，统一承载后台任务排队、认领、重试与调度器启停状态。
  18. 手动同步已从 API 进程内 `asyncio.create_task(...)` 改为数据库任务入队，`worker` 进程负责认领执行，`sync_job_log` 保留为执行日志与观测真源。
  19. 定时同步已从 FastAPI 生命周期中移出，独立 `scheduler` 进程仅负责按配置入队销售计划、BOM、生产订单、研究所、快照 reconcile 与 BOM 补数消费任务。
  20. `docker-compose.yml` 已新增 `worker` / `scheduler` 服务，后端容器启动链路已拆分为 `backend-entrypoint.sh`、`worker-entrypoint.sh`、`scheduler-entrypoint.sh`。
  21. `docs/openapi.json` 已重新导出，手动同步返回状态说明已同步为 `queued / running / noop` 口径。
  22. `drawing_status_sync_service.py` 已将发图状态回填改为批量加载 `detail_id` 与 `(order_no, material_no)` 两类研究所匹配数据，避免 `refresh_all` 逐单查询。
  23. 对同一 `(order_no, material_no)` 命中的多条研究所记录，现统一按 `drawing_release_date` 最新、再按 `id` 倒序选定回填来源，避免命中过旧发图日期。
  24. 用户管理集成测试已补“停用角色不能用于创建用户 / 更新用户角色”的边界回归，避免停用角色重新分配到活跃用户。
  25. 认证 / 保护链路已补“停用用户后现有 session 立即失效”和“管理员角色切换后现有 session 立即失去 admin 权限”的回归测试，覆盖角色变更与用户停用后的权限实时收口。
  26. `schedule_snapshot_refresh_service.py` 已在单条/批量刷新路径复用预加载 `OrderScheduleSnapshot`，避免 refresh 阶段按订单行再次 `find_by_order_line_id`，收口 snapshot 刷新中的隐藏 N+1。
  27. 已新增 `docs/自动排产系统-后端整改清单.md` 作为后端并行整改执行源，并通过 `docs/Agent.md` 明确为“只能勾选、不能改正文”的 checklist 规则。
  28. 本轮已在 `OrderScheduleSnapshot`、排产快照刷新、BOM 查询、部件排产、列表查询、导出与 OpenAPI 中统一引入 `plant`，并新增 Alembic 迁移 `20260326_0013_add_snapshot_plant.py` 回填历史 snapshot 工厂字段。
  29. `schedule_export_service.py` 已改为按批次读取 snapshot / part 结果并写入 `SpooledTemporaryFile`，CSV/XLSX 导出统一携带 `plant` 列，且在大文件场景下先做行数校验，避免写一半再失败。
  30. `background_task_worker_service.py` 已拆成“短事务读取任务上下文 → 处理器独立 session 执行 → 短事务回写成功/失败状态”的三段式执行边界，并补充失败后已提交变更不被整任务回滚的测试。
  31. 已新增 Alembic 迁移 `20260326_0014_add_pg_trgm_search_indexes.py`，在 PostgreSQL 下为排产快照、零件排产、销售计划、BOM、生产订单、整机周期历史等 `%keyword%` 高频模糊搜索字段补齐 `pg_trgm + GIN` 索引。
  32. 已新增 `scripts/validate_postgres_runtime.py` 并在真实 PostgreSQL 上执行通过，覆盖 `pg_trgm` 扩展、trigram 索引存在性、JSONB 落库回读、`FOR UPDATE SKIP LOCKED` 队列认领、事务级 advisory lock 与代表性模糊搜索执行计划验证。
  33. `schedule_snapshot_refresh_service.py` 已支持 `snapshot_refresh_batch_size`，known-order seed / rebuild 改为分批 ID + bulk upsert。
  34. `schedule_snapshot_refresh_service.py` 已把多入口 `order_line_id` 收口为 SQL `UNION`，并复用 BOM 去重结果。
  35. `schedule_export_service.py` 等已统一用 `count_for_export()` 做 XLSX 行数预检。
  36. `order_schedule_snapshot_repo.py` 等已把 snapshot observability 统计收口到聚合 SQL。
  37. `schedule_export_service.py` 与 `order_schedule_snapshot_repo.py` 已把 snapshot 导出改为流式批处理。
  38. `schedule_snapshot_refresh_service.py` 已在 fast seed / rebuild 复用共享 dynamic context。
  39. `bom_backfill_queue_repo.py` 与 `sync_job_observability_service.py` 已收口 BOM queue failure kind / latest failed 查询。
  40. `part_schedule_result_repo.py` 与 `schedule_export_service.py` 已把零件导出改为 snapshot + part 流式 join。
  41. `schedule_query_service.py`、`order_schedule_snapshot_repo.py`、`part_schedule_result_repo.py` 已将 dashboard 总览中的整机汇总、时间窗汇总、零件汇总改为复用聚合查询，减少查询侧重复统计 SQL。
  42. `/api/dashboard/overview` 已扩展返回排产/交付趋势（`day / week / month`）、事业群订单数量/金额聚合和异常整机订单池，继续保持单接口供 Dashboard 消费；相关 schema、测试与 `docs/openapi.json` 已同步收口。
  43. Docker Compose 启动时已收口为仅 `backend-entrypoint.sh` 负责 Alembic 迁移，`worker`/`scheduler` 不再并发跑迁移；`BackgroundTaskWorkerService` 也已改为单任务 claim、执行开始后再标记 started，并在 heartbeat 中续租 `claimed_at`，避免长任务被 stale recovery 误回收。`r`n  44. 自动同步调度器触发的 `schedule_snapshot_reconcile` 与 `bom_backfill_queue_consume` 现已在入队阶段创建 `sync_job_log`，worker 成功/空队列/失败路径也会正确收尾；同步日志页已能区分“快照对账 / BOM 补数消费 / 系统”口径，避免自动任务只留在 `background_task` 看不到日志。
  42. `schedule_query_service.py` 与 `order_schedule_snapshot_repo.py` 已将工作日历单日详情收口为单次聚合查询，避免按天详情页重复执行 summary + 多次列表查询。
  43. `test_background_task_worker_service.py` 已补 stale running task 的回收测试，覆盖“可重试任务回退 pending”和“超过最大重试后直接失败”两条 worker 恢复路径。
  44. `test_background_task_dispatch_service.py` 已补充 `dedupe_key` 去重回归，覆盖重复 enqueue 下的 task/job 去重与 scheduler / dispatch 边界场景。
  45. 零件周期基准已补齐 `plant / sample_count / source_updated_at` 字段、按“零件类型 + 机型 + 工厂”自动重建、后台任务去重、生产订单同步后自动入队、手工值保护与按影响范围批量刷新 snapshot，OpenAPI 与专项回归测试已同步收口。
  46. `test_sync_scheduler_service.py` 已补齐 scheduler control 心跳回写、stale 判停、跨实例 `mark_stopped` 忽略与同实例停机回写测试，worker / scheduler / dispatch 恢复链路的关键回归用例已形成闭环。
  47. 后台任务 / dispatch / scheduler / 导出链路已补齐首批可观测性日志与 failure_kind/stage 结构化进度解析，零件周期基准重建与生产订单联动的 sync log 文案已修复乱码。
  48. 已新增 `services/api/docs/worker-scheduler-snapshot-运维说明-2026-03-26.md`，按真实 Docker、脚本入口、观测接口、关键表与故障 SOP 沉淀任务系统运维说明。
  49. `bom_backfill_queue_service.py` 已将 BOM 自动补数候选识别收口为单次 sales plan / BOM 联合查询，并补齐“只走一次候选加载 SQL”的回归测试，减少候选识别阶段的重复查库。
  50. `app/openapi.py` 已补齐 `HTTPValidationError` / `ValidationError` 的 schema 与字段中文说明，异常查询、排产筛选与外源历史查询等缺失 description 的路由已补全，`docs/openapi.json` 可直接作为联调依据。
  51. `CurrentUserIdentity`、`/api/auth/login`、`/api/auth/session` 与受保护路由已补齐 `permission_codes` 和 `require_permission(...)` 运行期权限点骨架，admin 角色默认继承系统权限目录，后续可在不推翻现有链路的前提下继续扩展角色授权。
  52. BOM 层级计算已改为按父子路径逐行计算，并新增历史回填脚本；样例 80079439 / 30694926 / 10000131 已从层级 3 修复为 4。
  53. `sync_job_observability_service.py`、`order_schedule_snapshot_repo.py` 与 `part_schedule_result_repo.py` 已进一步收口同步观测摘要、整机 dashboard 摘要与零件 dashboard 摘要中的重复统计路径，剩余 SQL 热点已形成明确清单、优先级与验证结论。
  54. `order_schedule_snapshot_repo.py` 中的时间窗汇总、日历分布、单日汇总和单日详情等日历/时间窗聚合逻辑已继续拆到 `order_schedule_snapshot_calendar_helper.py`，repo 继续保留原 public 方法名做薄封装。
  55. `order_schedule_snapshot_repo.py` 中的 dashboard / observability 聚合逻辑已继续拆到 `order_schedule_snapshot_aggregate_helper.py`，repo 继续保留原 public 方法名做薄封装，`ScheduleQueryService` 与 snapshot 观测读口径保持不变。
  56. `order_schedule_snapshot_repo.py` 中的 dashboard / observability 聚合逻辑已继续拆到 `order_schedule_snapshot_aggregate_helper.py`，repo 继续保留原 public 方法名做薄封装，`ScheduleQueryService` 与 snapshot 观测读口径保持不变。
  57. `/api/admin/roles/{role_id}/permissions` now supports custom role-permission assignment; a role granted `user.view` can access the user list, while a role without `role.view` is still blocked from role-list access, proving runtime RBAC is now active beyond the original skeleton.
  58. `/api/admin/permission-matrix` 已提供角色列表、权限目录与逐角色绑定状态的矩阵视图，前端可直接消费该接口生成权限矩阵与按钮级联动说明。
  59. `/api/admin/permission-linkage` 已提供“权限点 -> 受保护后端接口”联动清单，前端可直接读取每个权限点对应的路由、方法、标签与摘要，生成权限矩阵说明、菜单显隐和按钮级联动依据。
  60. `schedule_constraint_read_service.py` 已作为库存/产能实时校验的统一读层接入 `ScheduleCheckService`，当前先输出非阻塞 `resource_snapshot`，后续扩展库存、产能或其他约束校验时无需重写现有排产检查入口。
  61. `schedule_snapshot_refresh_service.py` 中的动态上下文装配、批量预加载和日历/基准加载逻辑已拆到 `schedule_snapshot_refresh_context_loader.py`，快照刷新主链继续保留在 service 内，后续可继续按职责拆分 seed / refresh / observability 细节。
  62. `schedule_snapshot_refresh_service.py` 中的 runtime observation 与 snapshot observability 汇总逻辑已继续拆到 `schedule_snapshot_refresh_observability.py`，service 仅保留对 helper 的薄封装调用。
  63. `schedule_snapshot_refresh_service.py` 中的 known-order seed、批量 upsert、批次汇总与 ID batch 迭代逻辑已继续拆到 `schedule_snapshot_refresh_seed_helper.py`，service 继续保留原方法名作为薄封装，现有测试与调用方无需调整。
  64. `schedule_snapshot_refresh_service.py` 中按物料、机型、BOM 子件、零件类型和未来窗口解析 refresh 目标订单行的逻辑已继续拆到 `schedule_snapshot_refresh_target_resolver.py`，service 继续保留原 public 方法做薄封装。
  65. `schedule_snapshot_refresh_service.py` 中的动态检查刷新、整机结果刷新与 scheduled stale 判定逻辑已继续拆到 `schedule_snapshot_refresh_refresher.py`，service 继续保留原方法名做薄封装，外部行为与测试入口保持不变。
  66. `schedule_snapshot_refresh_service.py` 中的批量预加载与 known-order rebuild 协调逻辑已继续拆到 `schedule_snapshot_refresh_batch_helper.py`，service 保留 `_preload_refresh_batch_dependencies` 与 `_refresh_all_known_order_line_ids_in_batches` 作为薄封装，现有 monkeypatch 回归仍可直接复用。
  67. `schedule_snapshot_refresh_service.py` 中的 `_fast_seed_all` seed orchestration 已继续拆到 `schedule_snapshot_refresh_seed_orchestrator.py`，service 保留原方法名做薄封装，现有批处理回归与 monkeypatch 场景保持不变。
  68. `schedule_snapshot_refresh_service.py` 中的 `ensure_seeded / refresh_one_committed / refresh_future_window / rebuild_all_open_snapshots` 运行期 orchestration 已继续拆到 `schedule_snapshot_refresh_runtime_orchestrator.py`，service 继续保留原 public 方法名做薄封装。
  69. `schedule_snapshot_refresh_service.py` 中的 `refresh_one / refresh_batch / _refresh_one_prefetched` 执行壳已继续拆到 `schedule_snapshot_refresh_executor.py`，service 继续保留原 public 方法名和 monkeypatch 入口做薄封装。
  70. `schedule_snapshot_refresh_service.py` 中的 `refresh_by_material_no / product_model / bom_component_no / part_type` 公开入口与 `mark_scheduled / mark_scheduled_stale` 壳层已继续拆到 `schedule_snapshot_refresh_action_helper.py`，service 继续保留原 public 方法名与测试入口。
  71. `pending_delivery` 已从 `pending_drawing` 中独立出来，后端现在明确区分“缺少确认交货期”和“未完成发图”两类状态；相关检查、快照、导出状态文案、issue 留痕与 OpenAPI 说明已同步收口。
  72. `trigger_date`、`machine_cycle_days`、`machine_assembly_days`、`assembly_time_days`、`part_cycle_days` 与 `key_part_cycle_days` 的当前实现口径已在 schema、OpenAPI 与回归测试中明确：`trigger_date` 当前等同基于整机主周期倒排出的最晚开工日，`machine_assembly_days` 单独用于零件排产阶段预留总装窗口，`key_part_cycle_days` 作为部装组倒排锚点周期。
  73. 导出表头与管理端 schema 中关于整机主周期、整机总装时长、部装装配时长、零件自身周期和关键件倒排周期的对外口径已同步收口，避免用户看到的字段名继续延续旧的模糊表达。
  74. `machine_cycle_days / machine_assembly_days / assembly_time_days / part_cycle_days / key_part_cycle_days` 的对外口径已同步落到导出表头、管理端 schema、列表 schema 与 OpenAPI，当前实现语义已能在接口和导出两侧一致读出。
  75. `part_schedule_service.py` 已新增整机关联一致性守护：当 `machine_schedule_id` 不属于传入的 `order_line_id` 时，零件排产会显式拒绝执行，避免 `part_schedule_result` 写入跨订单错绑数据。
  76. `schedule_snapshot_refresh_service.py` 已新增 `machine_schedule_id -> order_line_id` 一致性守护：snapshot 标记 scheduled 前会校验整机排产结果是否属于当前订单，防止 `order_schedule_snapshot.machine_schedule_id` 写入跨订单错绑数据。
  77. `data_issue_repo.py` 已新增 `biz_key -> order_line_id` 一致性守护：订单型 issue 若传入 `order_line_id`，则 `biz_key` 必须与其一致；缺失时自动补齐为订单行字符串，避免 issue 留痕出现订单关联错链。
  78. `order_schedule_snapshot_repo.py` 已把 `machine_schedule_id -> order_line_id` 一致性检查下沉到 snapshot `upsert` 路径，避免未来除 `mark_scheduled(...)` 之外的其他写入链路绕过守护。
  79. `order_schedule_snapshot_repo.py` 已进一步收口状态与整机关联的一致性：非 `scheduled / scheduled_stale` 的 snapshot 若写入 `machine_schedule_id`，仓储层会统一清空该关联，避免未排产状态误挂整机结果。
  80. `OrderScheduleSnapshot` 模型层已新增状态-整机关联 validator：直接构造实体时，非 `scheduled / scheduled_stale` 状态会自动清空 `machine_schedule_id`，避免绕过仓储层后再次写入不一致状态。
  81. `DataIssueRepo.upsert_open_issue(...)` 已把订单型 issue 的自然键收口为 `issue_type + source_system + order_line_id + open`，不再因 `issue_title` 文案变化而插出第二条 open issue；现有记录会在标题变更时原地更新。
  82. `DataIssueRecord` 模型层已新增 `biz_key -> order_line_id` validator，直接构造实体时也会自动补齐订单键或拒绝错链，避免绕过仓储入口写入不一致的订单型 issue。
  83. `DataIssueRepo.upsert_open_issue(...)` 已支持自动收口历史重复 open issue：当同一订单型自然键下存在多条 open issue 时，会统一保留最新一条，并把更早的重复项自动标记为 resolved。
  84. `DataIssueRecord / DataIssueRepo` 已把 `source_system` 的空值统一归一到 `None`，避免 `None / "" / 空白字符串` 混用把非订单型 issue 的自然键再次打散。
  85. 管理端 `machine-cycle-baselines / assembly-times / part-cycle-baselines` 三组接口的 description 已同步改为当前实现口径，避免后台维护页和联调文档继续沿用旧的模糊字段解释。
## 项目架构

### 1. 技术栈
- FastAPI
- SQLAlchemy Async
- PostgreSQL
- Alembic
- APScheduler
- Docker Compose

### 2. 目录结构
- 后端目录：`auto-scheduling-system/services/api`
- Router：`app/routers`
- Service：`app/services`
- Repository：`app/repository`
- Scheduler：`app/scheduler`
- Sync：`app/sync`
- Common：`app/common`

### 3. 当前主链路
- 用户认证：`app/routers/auth_router.py`、`app/services/user_auth_service.py`、`app/common/auth.py`
- 用户管理：`app/routers/admin_user_router.py`
- 管理接口保护：`require_permission(...)` 权限点依赖
- 排产与查询：`schedule_query_router.py`、`admin_schedule_router.py`
- 外部同步：`admin_sync_router.py`、`sync_scheduler.py`、`app/sync/*`

## 功能开发进度

| 日期 | 模块 | 改动内容 | 目标/原因 | 验证方式 |
|---|---|---|---|---|
| 2026-03-30 | ?????????? | ?? `part_cycle_precision.py` ?? `cycle_days` / `unit_cycle_days` ??????? `part_cycle_baseline_repo.py`?`part_cycle_baseline_service.py`?`admin_part_cycle_router.py` ????? manual ???history ????????????? API / baseline ???? | ????????????????? 1 ???????????????????????????????????????????????? | `pytest services/api/tests/test_api/test_admin_part_cycle_api.py services/api/tests/test_baseline/test_part_cycle_baseline_service.py -q` 12 passed?`py_compile` ?? |
| 2026-03-30 | ???????? | ?? `assembly_time_repo.py` ? `FINAL_ASSEMBLY_NAME` ????????? `AssemblyTimeDefaultService.reconcile_final_assembly_sequence()`???????????????/???????????????? = ???????? + 1????????????????? | ????????????????????????? 1 ???????????? JH21-80 ?????????????? | `pytest services/api/tests/test_baseline/test_assembly_time_default_service.py services/api/tests/test_api/test_admin_assembly_time_api.py -q` 14 passed |
| 2026-03-30 | Docker / worker 队列修复 | 从 `worker-entrypoint.sh`、`scheduler-entrypoint.sh` 移除 `alembic upgrade head`，并在 `docker-compose.yml` 中让 `worker` / `scheduler` 等待 `backend` 健康；同时调整 `background_task_repo.py` 与 `background_task_worker_service.py`，将 worker 改为每轮只 claim 1 个任务、真正执行开始后再写 `started_at`，并在 heartbeat 中刷新 `claimed_at`；同步补齐 `test_background_task_worker_service.py` 回归 | 避免 Docker 启动阶段的并发迁移竞争，以及长任务执行中被 stale recovery 错误回收或未开始任务提前显示为 running | `pytest services/api/tests/test_services/test_background_task_worker_service.py -q` 7 passed；backend `py_compile` 校验通过 |
| 2026-03-30 | 自动调度同步日志补齐 | 在 `sync_scheduler.py` 中为 `schedule_snapshot_reconcile` 与 `bom_backfill_queue_consume` 补齐 `sync_job_kwargs`，在 `bom_backfill_queue_service.py` / `auto_bom_backfill_service.py` 中支持复用预创建日志，在 `background_task_worker_service.py` 中为快照对账与 BOM 补数消费补齐日志收尾；同步更新 `sync_job_observability_service.py` 与前端 `SyncLogListPage.vue` 的任务类型/来源展示口径 | 让自动同步调度器触发的任务不再只留在 `background_task`，而能像手动同步一样进入同步日志，便于页面查看、筛选和追踪执行结果 | `pytest services/api/tests/test_scheduler/test_sync_scheduler_service.py services/api/tests/test_services/test_background_task_worker_service.py services/api/tests/test_sync/test_auto_bom_backfill_service.py services/api/tests/test_api/test_admin_sync_api.py -q` 45 passed；`npm test`、`npm run lint`、`npm run build` 通过 |
| 2026-03-28 | Dashboard 总览扩展 | 在 `schedule_schemas.py`、`order_schedule_snapshot_aggregate_helper.py`、`order_schedule_snapshot_repo.py`、`schedule_query_service.py` 与 `schedule_query_router.py` 中扩展 `/api/dashboard/overview`，新增排产/交付趋势、事业群订单数量/金额聚合和异常整机订单池；同步补齐 `test_schedules_api.py` 与重导 `docs/openapi.json` | 为排产总览第二轮“计划 / 管理”双视角页面提供真实趋势、真实事业群统计和异常订单池真源，继续保持单接口读取模式 | `pytest services/api/tests/test_api/test_schedules_api.py -q` 27 passed；`py_compile` 校验通过；OpenAPI JSON 已重新导出 |
| 2026-03-23 | 后端目录迁移 | 后端工程从旧目录收口到 `auto-scheduling-system/services/api` | 统一工程结构和部署入口 | 本地测试通过 |
| 2026-03-23 | 正式身份表 | 新增 `app_role / app_user / app_user_role / user_session` 与对应 Alembic 迁移 | 为正式用户、角色和会话体系奠定基础 | Alembic 迁移 + API 测试通过 |
| 2026-03-23 | 正式认证接口 | 新增 `/api/auth/login | /api/auth/session | /api/auth/logout`，登录方式改为账号密码 + Cookie 会话 | 替换旧版管理员令牌认证 | `pytest tests/test_api/test_admin_auth_api.py -q` 通过 |
| 2026-03-23 | 角色保护 | 管理接口统一切到 `require_admin_role`，仅允许具备 `admin` 角色的正式用户访问 | 收口权限判定逻辑 | `pytest tests/test_api/test_admin_protection_api.py -q` 通过 |
| 2026-03-23 | 用户管理 API | 提供用户列表、创建、启停用、重置密码、角色更新接口 | 为前端“用户管理”页提供后端支撑 | `pytest tests/test_api/test_admin_user_api.py -q` 通过 |
| 2026-03-23 | 会话时间与存储安全 | 会话过期时间统一为 UTC ISO8601 `Z`；会话 token 改为哈希存储 | 修复时区偏差并降低明文会话风险 | `pytest tests/test_api/test_admin_auth_api.py tests/test_config/test_settings.py -q` 通过 |
| 2026-03-24 | 正式认证主链路收口 | `common/auth.py`、`auth_router.py` 不再回退旧管理员会话；`main.py` 已移除 `admin_auth_router` 挂载 | 不再保留旧版正式兼容逻辑 | `pytest tests/test_api/test_admin_auth_api.py tests/test_api/test_admin_protection_api.py tests/test_api/test_admin_user_api.py tests/test_config/test_settings.py -q` 26 passed |
| 2026-03-24 | OpenAPI 同步 | 修正 `main.py` 中认证说明、示例响应和安全方案文案，并重新导出 `docs/openapi.json` | 确保 Swagger / OpenAPI 与当前正式接口一致 | OpenAPI JSON 已重新生成 |
| 2026-03-25 | 认证边界收口 | 去掉新建用户空角色默认补 `admin` 与 bootstrap 弱默认密码；补非 admin 登录/访问边界测试；补 `/api/auth/login` 与受保护接口的 OpenAPI 401/403 错误响应并重新导出 `docs/openapi.json` | 收口正式用户体系的默认安全边界，避免误创建管理员用户并提升接口文档可用性 | `pytest tests/test_api/test_admin_auth_api.py tests/test_api/test_admin_protection_api.py tests/test_api/test_admin_user_api.py tests/test_config/test_settings.py -q` 通过；OpenAPI JSON 已重新生成 |
| 2026-03-25 | 浏览器认证联调回归 | 通过 Headless Chrome + Playwright 对运行中的 Docker 前端 / 后端执行真实认证链路回归，覆盖未登录拦截、登录回跳、404、用户管理页刷新保活、退出后二次拦截，并留痕到 `docs/regression/2026-03-25-认证主链路浏览器回归.md` | 补齐 `/api/auth/*` 正式链路在真实浏览器场景下的闭环验证，避免只依赖接口测试和构建结果判断完成 | 浏览器回归 11/11 通过 |
| 2026-03-25 | legacy 认证链路清理 | 删除 `app/common/admin_auth.py`、`app/models/admin_session.py`、`app/repository/admin_session_repo.py`、`app/routers/admin_auth_router.py`，同步移除 `AdminSession` 模型导出与旧认证 schema，并把正式会话 Cookie 配置 / OpenAPI 描述统一改为 `user_session` 命名 | 收口遗留管理员会话实现，避免运行时代码与文档继续混用旧 `admin session` 口径 | `pytest -q` 268 passed；全局搜索确认 `services/api/app` 下已无 legacy `admin session` 运行时引用；`docs/openapi.json` 已重新生成 |
| 2026-03-25 | 历史认证文档收口 | 更新 `真实联调操作清单`、Postman 指南、curl 示例，统一按 `/api/auth/*` + `user_session` 描述当前联调链路 | 避免历史文档继续把过时认证方式写成现行方案，降低后续联调与排查误判 | 文档全文复扫 `/api/admin/session`、`X-Admin-Token` 等关键词，当前联调文档已无误导性表述 |
| 2026-03-25 | 排产编排器拆分 | 新增 `schedule_orchestrator_helpers.py`，将 `schedule_orchestrator.py` 中的预校验失败响应组装、warning items 汇总、issue payload 构建与异常标记等纯辅助逻辑抽离，主编排流程保持不变 | 降低排产编排器单文件复杂度，在不改变后端逻辑的前提下提升可维护性 | `python -m py_compile` 通过；`pytest tests/test_scheduler/test_schedule_orchestrator.py -q` 10 passed；`pytest -q` 268 passed |
| 2026-03-25 | 快照刷新服务拆分 | 新增 `schedule_snapshot_refresh_helpers.py`，将 `schedule_snapshot_refresh_service.py` 中的快照 payload 组装、warning/issue flags 计算、stale 判断与部分纯辅助逻辑抽离，service 对外方法与主链路保持不变 | 降低排产快照服务单文件复杂度，在不改变后端逻辑的前提下提升可维护性 | `python -m py_compile` 通过；`pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 5 passed；`pytest -q` 268 passed |
| 2026-03-25 | 发图状态同步批量化优化 | 调整 `drawing_status_sync_service.py`：`refresh_all` 改为一次批量加载 `detail_id` 与 `(order_no, material_no)` 两类研究所匹配数据；同键多记录时优先选择最新 `drawing_release_date`、再以 `id` 倒序兜底 | 收口 4.4 首批同步热点，避免发图状态回填按订单逐条查询研究所数据，降低 N+1 风险并统一回填口径 | `pytest tests/test_sync/test_drawing_status_sync.py -q` 通过；`pytest -q` 274 passed |
| 2026-03-25 | 用户管理边界测试补强 | 在 `test_admin_user_api.py` 新增“停用角色不能用于创建用户 / 更新用户角色”集成测试，确保角色停用后不会被重新分配 | 收口 4.4 首批认证 / 角色 / 用户体系覆盖，降低停用角色重新流入用户体系的回归风险 | `pytest tests/test_api/test_admin_user_api.py -q` 通过；`pytest -q` 274 passed |
| 2026-03-25 | 认证会话与角色切换回归补强 | 在 `test_admin_auth_api.py` / `test_admin_protection_api.py` 新增“停用用户后现有 session 立即失效”和“管理员角色切换后现有 session 立即失去 admin 权限”集成测试 | 收口 4.4 首批认证 / 角色 / 用户体系覆盖，降低会话残留权限与角色变更后的访问回归风险 | `pytest tests/test_api/test_admin_auth_api.py tests/test_api/test_admin_protection_api.py -q` 23 passed；`pytest -q` 274 passed |
| 2026-03-25 | OpenAPI 模块拆分 | 新增 `app/openapi.py`，将 `main.py` 中的 OpenAPI 标签、描述、参数说明、响应示例与文档增强逻辑拆出，`main.py` 仅保留应用装配、生命周期、异常处理与路由挂载 | 降低 `main.py` 维护负担，在不改业务逻辑的前提下收口应用装配层职责 | `pytest tests/test_api/test_admin_auth_api.py tests/test_api/test_admin_protection_api.py -q` 21 passed；`pytest -q` 268 passed；校验 `/api/auth/login` 与 `/api/admin/users` 仍包含 401/403 响应 |
| 2026-03-25 | 任务系统生产化改造 | 新增 `background_task` / `sync_scheduler_state`、后台任务分发与 worker 执行服务，将手动同步改为数据库入队，将定时同步拆到独立 `scheduler` 进程；同步新增 `run_sync_worker.py`、`run_sync_scheduler.py` 与 Docker `worker` / `scheduler` 服务，并重新导出 `docs/openapi.json` | 收口 4.3“任务执行与生产化”待办，解决手动同步与定时同步仍绑在 API 进程、服务重启会中断任务的问题 | `pytest -q` 269 passed；`python -m pytest tests/test_api/test_admin_sync_api.py tests/test_scheduler/test_sync_scheduler_service.py -q` 17 passed；`docs/openapi.json` 已重新生成 |
| 2026-03-25 | Snapshot 刷新快照复用优化 | 调整 `order_schedule_snapshot_repo.py` 与 `schedule_snapshot_refresh_service.py`：`refresh_one / refresh_batch` 在已预加载 `OrderScheduleSnapshot` 的情况下直接复用内存中的 existing snapshot 做更新，不再在 upsert 阶段对每个订单行再次 `find_by_order_line_id` | 收口 4.4 snapshot 热点，避免单条刷新与批量 refresh 中的隐藏二次查库，降低快照重建与批量回刷时的 N+1 风险 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_exports_api.py -q` 18 passed；`pytest -q` 277 passed |
| 2026-03-26 | 整改清单文档治理 | 新增 `docs/自动排产系统-后端整改清单.md`，并同步更新 `docs/Agent.md` 约束两份整改清单只能勾选、不能改正文 | 为后端与前端并行整改提供统一执行真源，避免执行过程中口径漂移 | 文档创建完成，规则写入完成 |
| 2026-03-26 | plant 全链路 / 导出 / worker 事务边界整改 | 在 `config.py`、`plant_utils.py`、`order_schedule_snapshot.py`、`schedule_snapshot_refresh_service.py`、`schedule_snapshot_refresh_helpers.py`、`bom_relation_repo.py`、`schedule_check_service.py`、`assembly_identify_service.py`、`key_part_identify_service.py`、`part_schedule_service.py`、`schedule_orchestrator.py`、`order_schedule_snapshot_repo.py`、`part_schedule_result_repo.py`、`schedule_query_router.py`、`schedule_export_router.py`、`schedule_export_service.py`、`background_task_worker_service.py` 等链路完成 plant 统一透传、导出批处理与后台任务事务边界拆分，并新增 Alembic 迁移 `20260326_0013_add_snapshot_plant.py` 与对应回归测试 | 收口整改清单 P0 中“plant 必填维度统一”“导出链路大数据量内存控制”“后台任务长事务拆分”三类核心风险，避免不同工厂串 BOM / 串快照，减少导出内存峰值并缩小任务失败回滚范围 | `pytest -q` 279 passed；`docs/openapi.json` 已重新生成，已复核 `plant` 查询参数中文说明与导出接口说明 |
| 2026-03-26 | Snapshot seed / rebuild 热点收口 | 新增 `snapshot_refresh_batch_size`，known-order seed / rebuild 改为分批 ID + bulk upsert | 收口 P1-1，降低 snapshot 初始化压力 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 10 passed；`pytest tests/test_api/test_admin_schedule_api.py tests/test_api/test_schedules_api.py -q` 36 passed；`pytest -q` 282 passed |
| 2026-03-26 | Snapshot / BOM SQL 热点清理（第 1 批） | 将多入口 `order_line_id` 收口为 SQL `UNION`，并复用 BOM 去重结果 | 收口 P1-2 首批 snapshot / BOM 热点 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 13 passed；`pytest tests/test_api/test_admin_part_cycle_api.py tests/test_api/test_admin_schedule_api.py tests/test_sync/test_sync_workflow_service.py -q` 16 passed；`pytest -q` 285 passed |
| 2026-03-26 | 导出链路 SQL 热点清理（第 1 批） | XLSX 导出前统一走 `count_for_export()` 行数预检 | 收口 P1-2 首批导出热点 | `pytest tests/test_api/test_exports_api.py -q` 14 passed；`pytest tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py -q` 36 passed；`pytest -q` 287 passed |
| 2026-03-26 | 可观测性 SQL 热点清理（第 1 批） | snapshot observability 与 latest jobs 统计改为聚合查询 | 收口 P1-2 首批可观测性热点 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 14 passed；`pytest tests/test_api/test_admin_sync_api.py -q` 14 passed；`pytest -q` 289 passed |
| 2026-03-26 | Snapshot 批量刷新热点清理（第 2 批） | fast seed / rebuild 复用 shared dynamic context | 收口 P1-2 第二批 snapshot 热点 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 16 passed；`pytest tests/test_api/test_admin_schedule_api.py tests/test_api/test_schedules_api.py -q` 36 passed；`pytest -q` 291 passed |
| 2026-03-26 | 可观测性 SQL 热点清理（第 2 批） | BOM queue failure kind / latest failed 改为单次聚合查询 | 收口 P1-2 第二批 sync / BOM queue 热点 | `pytest tests/test_api/test_admin_sync_api.py -q` 14 passed；`pytest -q` 291 passed |
| 2026-03-26 | 导出链路 SQL 热点清理（第 2 批） | 零件导出改为 snapshot + part 流式 join，统一导出筛选口径 | 收口 P1-2 中零件导出残余热点，避免影响普通列表语义 | `pytest tests/test_api/test_exports_api.py -q` 15 passed；`pytest tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py -q` 36 passed；`pytest tests -q` 292 passed |
| 2026-03-26 | Dashboard / 查询侧 SQL 热点清理（第 1 批） | 在 `order_schedule_snapshot_repo.py`、`part_schedule_result_repo.py`、`schedule_query_service.py` 中新增 dashboard 聚合方法，收口整机汇总、today/week/month 时间窗汇总与零件汇总查询，并补 service 级回归测试 | 收口 P1-2 中 dashboard / 查询侧的重复统计热点，降低总览页串行查询次数且不改变接口结构 | `pytest tests/test_api/test_schedules_api.py -q` 26 passed；`pytest tests/test_api/test_admin_schedule_api.py -q` 11 passed；`pytest tests -q` 293 passed |

| 2026-03-26 | 工作日历日详情 SQL 热点清理 | 在 `order_schedule_snapshot_repo.py`、`schedule_query_service.py` 中新增 `get_calendar_day_detail()` 单查询聚合路径，统一收口 `confirmed_delivery_date / trigger_date / planned_start_date` 的单日详情查询，并补 service 级回归测试 | 收口 P1-2 中查询侧工作日历日详情的重复 SQL，避免单日详情页重复执行 summary + 多次列表查询且不改变接口结构 | `pytest tests/test_api/test_schedules_api.py -q` 27 passed；`pytest tests/test_api/test_admin_work_calendar_api.py -q` 5 passed；`pytest tests -q` 297 passed |

| 2026-03-26 | worker stale task 恢复测试补强 | 在 `tests/test_services/test_background_task_worker_service.py` 中新增 stale running task 回收测试，覆盖“可重试任务回退 pending 并恢复 queued job”与“超过最大重试次数后任务失败并落 completed_with_errors job”两条恢复路径 | 收口 P1 中后台任务恢复测试的首批关键场景，避免 worker 只在理想执行路径下有回归覆盖 | `pytest tests/test_services/test_background_task_worker_service.py -q` 4 passed；`pytest tests -q` 299 passed |

| 2026-03-26 | 后台任务 dispatch 去重回归补强 | 新增 `tests/test_services/test_background_task_dispatch_service.py`，覆盖 `dedupe_key` 生效时的重复入队、task/job 去重与 scheduler / worker 分发边界 | 收口 P1 中 scheduler / dispatch 链路的关键去重场景，避免重复任务污染 task 与 sync job log | `pytest tests/test_services/test_background_task_dispatch_service.py tests/test_scheduler/test_sync_scheduler_service.py tests/test_services/test_background_task_worker_service.py -q` 通过；`pytest tests -q` 302 passed |

| 2026-03-26 | scheduler control 心跳/停机回归补强 | 在 `tests/test_scheduler/test_sync_scheduler_service.py` 中新增 scheduler control 心跳回写、fresh/stale 状态判定、跨实例 `mark_stopped` 忽略与同实例停机回写测试，并串联 worker stale recovery + dispatch dedupe 的现有验证结果 | 收口整改清单中“worker / scheduler / 后台任务恢复测试补齐”的关键验收场景，避免任务系统只覆盖理想运行路径 | `pytest tests/test_scheduler/test_sync_scheduler_service.py -q` 9 passed；`pytest tests -q` 307 passed |

| 2026-03-26 | 可观测性补强（第 1 批） | 在 `background_task_worker_service.py`、`background_task_dispatch_service.py`、`sync_scheduler.py`、`schedule_export_service.py` 中补齐后台任务/调度器/导出链路的开始、完成、失败与去重日志，新增 failure_kind / stage / task_id 失败归类；同步扩展 `sync_job_observability_service.py` 对失败上下文与零件周期基准重建结果的结构化解析，并修复 worker 中已出现的乱码 sync log 文案 | 收口整改清单中“强化可观测性”的首批关键场景，让线上排查可以定位到任务、阶段、导出规模与失败原因，同时避免 sync log 文案继续乱码 | `pytest tests/test_services/test_background_task_worker_service.py tests/test_services/test_background_task_dispatch_service.py tests/test_services/test_schedule_export_service.py tests/test_services/test_sync_job_observability_service.py tests/test_scheduler/test_sync_scheduler_service.py -q` 23 passed；`pytest tests -q` 314 passed |

| 2026-03-26 | 任务系统运维说明沉淀 | 新增 `services/api/docs/worker-scheduler-snapshot-运维说明-2026-03-26.md`，基于真实 `docker-compose.yml`、entrypoint、run script、观测接口与关键表，沉淀 backend / worker / scheduler / snapshot 的启动方式、观察入口、日志关键词与故障处理 SOP | 收口整改清单中“worker / scheduler / snapshot 运维说明”，让后端同学接手任务系统时能直接按文档完成启动、观察与排障 | 文档逐条对照 `docker-compose.yml`、`docker/*entrypoint.sh`、`scripts/run_sync_worker.py`、`scripts/run_sync_scheduler.py`、`app/main.py`、`app/sync_scheduler.py`、`app/services/background_task_worker_service.py`、`app/services/schedule_snapshot_refresh_service.py`、`app/routers/admin_sync_router.py`、`app/routers/admin_schedule_router.py` 复核通过 |
| 2026-03-26 | BOM 回填候选 SQL 热点清理（第 3 批） | 调整 `bom_backfill_queue_service.py` 的 `_load_candidates()`，将原先按同条件分别查询 `order_id` 与 `(material_no, plant)` 的两次 SQL 合并为一次候选查询，再在 Python 侧整理唯一 `order_ids` 与 `(material_no, plant)`；同步在 `tests/test_sync/test_auto_bom_backfill_service.py` 补充“候选加载只走一次 sales plan/BOM 组合查询”的回归测试 | 继续收口整改清单中导出 / BOM / snapshot / 可观测性链路剩余 SQL 热点，减少 BOM 自动补数候选识别阶段的重复查库，同时不改变候选去重口径与接口行为 | `pytest tests/test_sync/test_auto_bom_backfill_service.py -q` 9 passed；`pytest tests -q` 316 passed |

| 2026-03-26 | 零件周期基准按工厂维度自动重建闭环 | 为 `part_cycle_baseline` 新增 `plant / sample_count / source_updated_at`、唯一约束与空工厂兜底唯一索引；新增后台任务 `part_cycle_baseline_rebuild` 与手动接口 `POST /api/admin/part-cycle-baselines/rebuild`；生产订单同步成功后自动入队；自动值仅在 `sample_count >= 3` 且 `max_cycle / min_cycle <= 2` 时正式入库，且不覆盖 `cycle_source = manual`；零件周期匹配补齐 plant 维度优先级，snapshot 改为按受影响范围批量刷新 | 收口“零件周期基准”自动生成的性能、冗余数据与并发风险，形成“同步完成 -> 异步重建 -> 受影响排产刷新”的闭环，并补齐页面展示与 OpenAPI 口径 | `pytest tests/test_baseline/test_part_cycle_baseline_service.py tests/test_api/test_admin_part_cycle_api.py tests/test_services/test_background_task_worker_service.py tests/test_scheduler/test_key_part_identify_service.py tests/test_scheduler/test_part_schedule_service.py tests/test_api/test_admin_baseline_delete_api.py tests/test_api/test_admin_schedule_api.py tests/test_scheduler/test_schedule_orchestrator.py -q` 通过；`python -m py_compile services/api/app/baseline/part_cycle_baseline_service.py services/api/app/services/background_task_worker_service.py services/api/app/scheduler/key_part_identify_service.py services/api/app/scheduler/part_schedule_service.py services/api/app/routers/admin_part_cycle_router.py` 通过；`docs/openapi.json` 已同步更新 |

| 2026-03-26 | 零件周期基准入库阈值放宽 | 将自动入库最低样本数从 `sample_count >= 3` 改为 `sample_count >= 1`，保留 `max_cycle / min_cycle <= 2` 波动率限制，使生产订单样本稀疏时也能形成零件基准周期首批基准值 | 响应“同步后页面仍无数据”的实际业务场景，先确保历史样本可落库可见，后续再通过 sample_count 与 confidence_level 识别低置信度数据 | `pytest tests/test_baseline/test_part_cycle_baseline_service.py tests/test_api/test_admin_part_cycle_api.py -q` 9 passed |
| 2026-03-26 | session 安全边界收口 | 在 `user_session_repo.py` 中新增活跃会话批量撤销能力，并在 `auth_router.py`、`admin_user_router.py` 中落地“新登录覆盖旧登录”“密码重置撤销会话”“停用用户回收会话”；同步补齐 API 级回归测试与接口说明 | 收口整改清单中“强化 session 安全”的关键边界策略，避免并发登录残留、密码变更后旧会话继续有效、停用用户后旧会话未主动回收 | `pytest tests/test_api/test_admin_auth_api.py tests/test_api/test_admin_user_api.py -q` 22 passed；`pytest tests -q` 318 passed |
| 2026-03-26 | OpenAPI 收口 | 在 `app/openapi.py` 中补齐 `HTTPValidationError` / `ValidationError` 的 schema 与字段中文说明，并为异常查询、排产筛选、整机周期历史、生产订单历史等缺少 description 的路由补充业务说明；重新导出 `docs/openapi.json` 并补齐 OpenAPI 相关测试 | 收口整改清单中“OpenAPI 收口”项，确保 Swagger / OpenAPI 可直接作为联调依据，而不是只能看概览 | `pytest tests/test_api/test_admin_auth_api.py tests/test_api/test_admin_protection_api.py -q` 25 passed；`pytest tests -q` 319 passed；`docs/openapi.json` 已重新导出 |

| 2026-03-26 | 导出链路 SQL 热点清理（第 3 批） | 将 Excel 导出前的全量 `count_for_export()` 行数校验改为 `max_rows + 1` 的 limit 探测，仅判断是否超限，不再在大表导出前额外执行完整 count；同步在 `order_schedule_snapshot_repo.py`、`part_schedule_result_repo.py` 中补齐超限探测方法，并更新导出测试覆盖“走 limit 探测而不是全量 count” | 继续收口整改清单中导出链路剩余 SQL 热点，减少大数据量 Excel 导出前的额外扫描成本，同时不改变导出文件内容、筛选口径与超限报错行为 | `pytest tests/test_api/test_exports_api.py tests/test_services/test_schedule_export_service.py -q` 17 passed；`pytest tests -q` 319 passed |
| 2026-03-26 | 快照观测 SQL 热点清理（第 3 批） | 将排产快照观测摘要中的 `known_order_count` 合并进 `order_schedule_snapshot_repo.py` 的聚合查询结果，避免 `get_observability_summary()` 在读取 snapshot 总数、状态分布、刷新来源后再额外执行一次已知订单 union-count；同步补测试覆盖“已知订单数走聚合结果，不再单独计数” | 继续收口整改清单中 snapshot / 可观测性链路剩余 SQL 热点，减少快照观测接口的额外统计查询，同时不改变观测返回结构与 coverage 计算口径 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed；`pytest tests -q` 319 passed |
| 2026-03-26 | RBAC 权限点控制预案 | 在 `common/auth.py`、`user_session_repo.py`、`user_account_repo.py`、`user_auth_service.py`、`auth_schemas.py`、`main.py` 与各受保护 router 中补齐 `permission_codes` 身份载荷、`require_permission(...)` 依赖工厂与按路由映射的权限点控制；同步更新 OpenAPI 403 说明与登录/会话响应示例，并补充权限保护回归测试 | 收口整改清单中“RBAC 从 require_admin_role 升级到权限点控制预案”项，在保持 admin 兼容的前提下把运行期鉴权升级为可扩展的权限点骨架，为后续角色授权与前端按钮级能力扩展预留入口 | `pytest tests/test_api/test_admin_auth_api.py tests/test_api/test_admin_protection_api.py tests/test_api/test_admin_user_api.py tests/test_api/test_admin_sync_api.py tests/test_api/test_admin_schedule_api.py -q` 61 passed；`pytest tests -q` 319 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | BOM 层级错误修复 | 修正 bom_sync_service 的层级算法，改为按父子路径逐行计算；新增 backfill_bom_levels.py 并完成历史全量回填 | 根治同一物料号在不同分支不同深度时复用错误层级的问题 | 测试：`pytest tests/test_sync/test_bom_sync.py -q` 4 passed，`pytest tests/test_api/test_data_bom_api.py -q` 7 passed；回填 82 组 / 35285 行 / 4356 行；样例 `80079439 / 30694926 / 10000131` 已从 3 修复为 4；服务已重建并健康检查 200 |
| 2026-03-27 | 剩余 SQL 热点清单收口 | 调整 `sync_job_observability_service.py` 的摘要统计，改为按 1 次快照聚合 + 其余 1 行子查询结果装配；同步收紧 `order_schedule_snapshot_repo.py` 与 `part_schedule_result_repo.py` 的 dashboard 摘要聚合路径，减少整机/零件 dashboard 的重复 round-trip，并补齐剩余热点的优先级与验证结论 | 收口整改清单中“继续清理导出/BOM/snapshot/可观测性链路剩余 SQL 热点”项，在不改接口结构与业务口径的前提下进一步压缩统计类查询路径，并把剩余风险转入后续结构/语义治理 | `pytest tests/test_api/test_admin_schedule_api.py tests/test_api/test_schedules_api.py tests/test_api/test_admin_sync_api.py tests/test_services/test_sync_job_observability_service.py -q` 54 passed |
| 2026-03-27 | 角色权限分配落地 | 在 `auth_schemas.py`、`admin_user_router.py`、`user_auth_service.py` 与 `role_repo.py` 中新增角色权限更新请求体、`PUT /api/admin/roles/{role_id}/permissions` 接口、权限存在性校验与角色详情强制刷新；系统内置角色仍禁止通过接口改权，自定义角色已支持按权限点分配能力；同步重导 `docs/openapi.json` | 把 RBAC 从“权限点骨架/预案”推进到“自定义角色可分配权限并影响运行期鉴权”的可运行状态，为后续权限矩阵、前端按钮级联动和中文角色口径收口提供后端基础 | `pytest tests/test_api/test_admin_user_api.py tests/test_api/test_admin_protection_api.py tests/test_api/test_admin_auth_api.py -q` 39 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | 权限矩阵视图落地 | 在 `auth_schemas.py` 与 `admin_user_router.py` 中新增权限矩阵响应模型和 `GET /api/admin/permission-matrix` 接口，返回角色列表、权限目录以及逐角色绑定状态；同步补齐“仅有 `user.view` 的自定义角色能看用户列表，但仍不能访问矩阵接口”的权限保护回归，并重导 `docs/openapi.json` | 为前端权限矩阵、角色配置页和按钮级权限联动提供可直接消费的后端真源，避免前端自行拼装 `/roles` + `/permissions` + `/roles/{id}/permissions` 三段数据 | `pytest tests/test_api/test_admin_user_api.py tests/test_api/test_admin_protection_api.py tests/test_api/test_admin_auth_api.py -q` 40 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | 权限点联动清单落地 | 在 `auth_schemas.py` 与 `admin_user_router.py` 中新增权限联动响应模型和 `GET /api/admin/permission-linkage` 接口，按运行时 `require_permission(...)` 元数据自动汇总“权限点 -> 受保护接口”映射；同步补齐“仅有 `user.view` 的自定义角色仍不能访问联动清单接口”的权限保护回归，并重导 `docs/openapi.json` | 为前端权限矩阵说明、菜单显隐和按钮级权限联动提供统一后端真源，避免继续手工维护权限点与页面/接口关系表 | `pytest tests/test_api/test_admin_user_api.py tests/test_api/test_admin_protection_api.py tests/test_api/test_admin_auth_api.py -q` 41 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | 库存/产能统一读层预留 | 新增 `services/schedule_constraint_read_service.py`，并在 `schedule_check_service.py` 中统一产出非阻塞 `resource_snapshot`，按物料/工厂和机型/日期窗口预留库存与产能校验读取入口；当前不改变既有排产结果，只为未来约束接入固定读层边界；同步补 `test_schedule_check_service.py` 回归 | 收口整改清单中“为库存/产能实时校验预留统一读层”项，确保后续接入库存、产能或其他资源约束时，不必重写现有排产检查主链路 | `pytest tests/test_scheduler/test_schedule_check_service.py tests/test_api/test_admin_schedule_api.py -q` 16 passed |
| 2026-03-27 | snapshot 动态上下文加载拆分 | 新增 `services/schedule_snapshot_refresh_context_loader.py`，把 `schedule_snapshot_refresh_service.py` 中的销售订单/整机结果/开放异常预加载、BOM 对、整机周期基准、日历装配和 shared dynamic context 构建逻辑下沉到独立 loader；service 仅保留薄封装与刷新主链编排 | 持续收口整改清单中“继续拆分超大文件”，降低 snapshot 刷新服务的单文件复杂度，并为后续继续拆分 seed / refresh / observability 细节预留明确边界 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed |
| 2026-03-27 | snapshot observability 辅助拆分 | 新增 `services/schedule_snapshot_refresh_observability.py`，把 snapshot observability 摘要装配、runtime observation 记录/重置/列表与耗时计算从 `schedule_snapshot_refresh_service.py` 中抽离；service 保持既有对外方法与调用口径不变 | 持续收口整改清单中“继续拆分超大文件”，进一步降低 snapshot 刷新服务的杂糅职责，方便后续继续拆 seed / refresh / observability 主体 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed |
| 2026-03-27 | snapshot seed/batch helper 拆分 | 新增 `services/schedule_snapshot_refresh_seed_helper.py`，把 known-order ID 合并、batch 迭代、seed row 构建、bulk upsert 和 refresh summary merge 等辅助逻辑从 `schedule_snapshot_refresh_service.py` 中抽离；service 保留原方法名做薄封装 | 持续收口整改清单中“继续拆分超大文件”，进一步降低 snapshot 刷新服务的单文件复杂度，并为后续继续拆 refresh 主体和状态语义治理预留边界 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed |
| 2026-03-27 | snapshot refresh target 解析拆分 | 新增 `services/schedule_snapshot_refresh_target_resolver.py`，把 `refresh_by_material_no / product_model / bom_component_no / part_type / future_window` 背后的订单行 ID 解析逻辑从 `schedule_snapshot_refresh_service.py` 中抽离；service 保留既有 public 方法名和返回结构不变 | 持续收口整改清单中“继续拆分超大文件”，进一步压缩 snapshot 刷新服务的查询解析职责，并为后续继续拆 refresh 主体与语义治理预留边界 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed |
| 2026-03-27 | snapshot refresh 主体辅助拆分 | 新增 `services/schedule_snapshot_refresh_refresher.py`，把动态检查刷新、整机结果刷新和 scheduled stale 判定逻辑从 `schedule_snapshot_refresh_service.py` 中抽离；service 仅保留对 refresher 的薄封装调用 | 持续收口整改清单中“继续拆分超大文件”，进一步压缩 snapshot 刷新服务的刷新装配职责，为后续继续收口语义治理和 refresh 主链编排边界提供基础 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed |
| 2026-03-27 | snapshot batch 协调辅助拆分 | 新增 `services/schedule_snapshot_refresh_batch_helper.py`，把批量预加载和 known-order rebuild 协调逻辑从 `schedule_snapshot_refresh_service.py` 中抽离；service 继续保留原方法名作为薄封装，保证现有测试和调用方不受影响 | 持续收口整改清单中“继续拆分超大文件”，进一步压缩 snapshot 刷新服务的批处理协调职责，并保持回归覆盖不变 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed |
| 2026-03-27 | snapshot seed orchestration 拆分 | 新增 `services/schedule_snapshot_refresh_seed_orchestrator.py`，把 `_fast_seed_all` 中 shared dynamic context 复用、预加载、seed row 构建与 bulk upsert 的编排流程从 `schedule_snapshot_refresh_service.py` 中抽离；service 继续保留 `_fast_seed_all` 作为薄封装 | 持续收口整改清单中“继续拆分超大文件”，进一步压缩 snapshot 刷新服务的 seed 编排职责，同时保持现有回归和 monkeypatch 习惯不变 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 27 passed |
| 2026-03-27 | snapshot runtime orchestration 拆分 | 新增 `services/schedule_snapshot_refresh_runtime_orchestrator.py`，把 `ensure_seeded / refresh_one_committed / refresh_future_window / rebuild_all_open_snapshots` 的运行期 orchestration、锁与观测包装从 `schedule_snapshot_refresh_service.py` 中抽离；service 保留原 public 方法名与测试入口不变 | 持续收口整改清单中“继续拆分超大文件”，进一步压缩 snapshot 刷新服务的外层 orchestration 壳层，同时保持既有回归与 monkeypatch 习惯稳定 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 28 passed |
| 2026-03-27 | snapshot execution shell 拆分 | 新增 `services/schedule_snapshot_refresh_executor.py`，把 `refresh_one / refresh_batch / _refresh_one_prefetched` 的执行壳和批次统计从 `schedule_snapshot_refresh_service.py` 中抽离；service 保留既有方法名与测试入口不变 | 持续收口整改清单中“继续拆分超大文件”，进一步降低 snapshot 刷新服务的执行编排壳层复杂度，并保持现有 monkeypatch 回归路径稳定 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 28 passed |
| 2026-03-27 | snapshot public action 壳层拆分 | 新增 `services/schedule_snapshot_refresh_action_helper.py`，把 `refresh_by_*` 系列公开入口与 `mark_scheduled / mark_scheduled_stale` 壳层从 `schedule_snapshot_refresh_service.py` 中抽离；service 保留既有 public 方法名和 monkeypatch 入口不变 | 持续收口整改清单中“继续拆分超大文件”，进一步压缩 snapshot 刷新服务的 public action 壳层，同时保持对外行为与回归路径稳定 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_admin_schedule_api.py -q` 28 passed |
| 2026-03-27 | snapshot 日历/时间窗聚合拆分 | 新增 `repository/order_schedule_snapshot_calendar_helper.py`，把 `order_schedule_snapshot_repo.py` 中的时间窗计数、日历分布、单日汇总与单日详情聚合逻辑抽离；repo 保留既有 public 方法名与 `ScheduleQueryService` 调用口径不变 | 持续收口整改清单中“继续拆分超大文件”，把当前最大的 snapshot 查询仓储进一步按职责切开，同时保持 schedules/admin-schedule 读接口行为稳定 | `pytest tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py -q` 38 passed |
| 2026-03-27 | snapshot dashboard/observability 聚合拆分 | 新增 `repository/order_schedule_snapshot_aggregate_helper.py`，把 `order_schedule_snapshot_repo.py` 中的 dashboard 摘要、status/warning/refresh-source 聚合、refresh bounds、planned-end month 统计与 snapshot observability 聚合逻辑抽离；repo 保留既有 public 方法名与 `ScheduleQueryService` / snapshot observability 调用口径不变 | 持续收口整改清单中“继续拆分超大文件”，把当前最大的 snapshot 查询仓储进一步按“日历/时间窗”和“dashboard/observability”两组职责切开，同时保持读接口与回归行为稳定 | `pytest tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_repository/test_order_schedule_snapshot_repo.py -q` 59 passed |
| 2026-03-27 | snapshot dashboard/observability 聚合拆分 | 新增 `repository/order_schedule_snapshot_aggregate_helper.py`，把 `order_schedule_snapshot_repo.py` 中的 dashboard 摘要、status/warning/refresh-source 聚合、refresh bounds、planned-end month 统计与 snapshot observability 聚合逻辑抽离；repo 保留既有 public 方法名与 `ScheduleQueryService` / snapshot observability 调用口径不变 | 持续收口整改清单中“继续拆分超大文件”，把当前最大的 snapshot 查询仓储进一步按“日历/时间窗”和“dashboard/observability”两组职责切开，同时保持读接口与回归行为稳定 | `pytest tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 55 passed |
| 2026-03-27 | `pending_delivery` 语义收口 | 在 `common/enums.py`、`schedule_check_service.py`、`schedule_orchestrator.py`、`schedule_snapshot_refresh_helpers.py`、`schedule_export_service.py` 与相关测试中新增并收口 `pending_delivery`，将“缺少确认交货期”与“未完成发图”从原 `pending_drawing` 多义状态中拆分；同步重导 `docs/openapi.json` | 收口整改清单中 P2 的首个高价值语义冲突点，确保状态值在排产、快照、查询、导出与 issue 留痕中不再一值多义，为后续 `trigger_date / 周期字段` 语义治理打基础 | `pytest tests/test_common/test_enums.py tests/test_scheduler/test_schedule_check_service.py tests/test_scheduler/test_schedule_orchestrator.py tests/test_scheduler/test_schedule_snapshot_refresh_service.py tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py -q` 74 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | `trigger_date / 周期字段` 语义收口 | 在 `app/openapi.py` 与 `schedule_schemas.py` 中补齐 `trigger_date / machine_cycle_days / machine_assembly_days / assembly_time_days / part_cycle_days / key_part_cycle_days` 的精确中文说明，并在 `test_machine_schedule_service.py`、`test_part_schedule_service.py` 中锁住当前实现口径：`trigger_date == planned_start_date`，零件排产会在整机交期前先预留总装窗口 | 收口整改清单中 P2 的第二个高价值语义冲突点，避免周期/触发日期字段继续在接口、导出和排产实现中出现“同名不同义” | `pytest tests/test_scheduler/test_machine_schedule_service.py tests/test_scheduler/test_part_schedule_service.py tests/test_common/test_enums.py tests/test_scheduler/test_schedule_check_service.py tests/test_scheduler/test_schedule_orchestrator.py tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py -q` 63 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | 导出/管理端周期字段口径收口 | 在 `schedule_export_service.py` 中把导出表头统一改为“整机主周期(天) / 整机总装时长(天) / 部装装配时长(天) / 零件自身周期(天) / 关键件倒排周期(天)”，并同步收紧 `admin_schemas.py` 中整机周期、装配时长、零件周期相关字段的中文说明；`docs/openapi.json` 已重导 | 把字段语义收口从“内部实现和 API 说明”推进到“用户可见导出与管理配置文案”，减少同字段在接口、导出和后台维护页上的口径漂移 | `pytest tests/test_api/test_exports_api.py tests/test_scheduler/test_machine_schedule_service.py tests/test_scheduler/test_part_schedule_service.py tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py -q` 58 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | 周期字段对外口径继续收口 | 在 `schedule_export_service.py`、`admin_schemas.py`、`schedule_schemas.py` 与 `app/openapi.py` 中继续统一整机主周期、整机总装时长、部装装配时长、零件自身周期和关键件倒排周期的对外说明，并补导出/排产定向回归，确保用户可见文案和接口说明保持一致 | 把字段语义收口从“导出表头和单点说明”推进到“导出、管理配置、列表 schema 与 OpenAPI 同口径”，进一步降低对外语义漂移风险 | `pytest tests/test_api/test_exports_api.py tests/test_scheduler/test_machine_schedule_service.py tests/test_scheduler/test_part_schedule_service.py tests/test_api/test_schedules_api.py tests/test_api/test_admin_schedule_api.py -q` 58 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | 管理端周期/时长接口说明收口 | 在 `admin_machine_cycle_router.py`、`admin_assembly_time_router.py` 与 `admin_part_cycle_router.py` 中把整机主周期、整机总装时长、部装装配时长和零件自身周期的 description 改成与当前实现一致，并补 OpenAPI 回归锁定这些说明 | 把字段语义收口从“列表 schema / 导出 / OpenAPI 字段描述”推进到“后台管理接口 description”，减少前后端和运维同学阅读接口说明时的歧义 | `pytest tests/test_api/test_admin_protection_api.py tests/test_api/test_admin_auth_api.py -q` 26 passed；`docs/openapi.json` 已重新导出 |
| 2026-03-27 | 零件排产整机关联一致性守护 | 在 `part_schedule_service.py` 中新增 `machine_schedule_id -> order_line_id` 一致性校验，显式阻止零件排产写入跨订单错绑的 `part_schedule_result`；同步补齐错绑场景回归测试 | 作为整改清单中 P3“约束、软关联与一致性检查”的首个落地点，把原本仅靠调用方约定的软关联关系升级为运行期守护，降低脏数据落库风险 | `pytest tests/test_scheduler/test_part_schedule_service.py -q` 4 passed |
| 2026-03-27 | snapshot 整机关联一致性守护 | 在 `schedule_snapshot_refresh_service.py` 的 `mark_scheduled(...)` 中新增 `machine_schedule_id -> order_line_id` 校验，显式阻止 snapshot 标记 scheduled 时引用跨订单的整机排产结果；同步补齐错绑场景回归测试 | 作为整改清单中 P3“约束、软关联与一致性检查”的第二个落地点，把 `order_schedule_snapshot.machine_schedule_id` 从软约定升级为运行期守护，降低 snapshot 与整机结果错链带来的状态污染风险 | `pytest tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 17 passed |
| 2026-03-27 | DataIssue 订单键一致性守护 | 在 `data_issue_repo.py` 中新增 `biz_key -> order_line_id` 一致性归一与校验：订单型 issue 传入 `order_line_id` 时会自动补齐或校验 `biz_key`，显式阻止订单 issue 留痕出现键值错链；同步补仓储级回归与 issues API 回归 | 作为整改清单中 P3“约束、软关联与一致性检查”的第三个落地点，把 `DataIssueRecord` 的订单型软关联从调用方约定升级为仓储层统一守护，降低 issue 查询与快照关联污染风险 | `pytest tests/test_repository/test_data_issue_repo.py tests/test_api/test_issues_api.py -q` 8 passed |
| 2026-03-27 | snapshot 仓储层整机关联一致性守护 | 在 `order_schedule_snapshot_repo.py` 中新增 `machine_schedule_id -> order_line_id` 归一校验，并补齐仓储级回归测试；无论是 `mark_scheduled(...)` 还是其他 snapshot upsert 路径，只要写入 `machine_schedule_id` 都会校验整机结果归属 | 作为整改清单中 P3“约束、软关联与一致性检查”的第四个落地点，把 snapshot 的整机关联守护从 service 单点保护升级为仓储层统一保护，进一步降低跨订单错链风险 | `pytest tests/test_repository/test_order_schedule_snapshot_repo.py tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 19 passed |
| 2026-03-27 | snapshot 状态-整机关联一致性收口 | 在 `order_schedule_snapshot_repo.py` 中新增规则：只有 `scheduled / scheduled_stale` 状态允许保留 `machine_schedule_id`，其他状态写入时会自动清空该关联；同步补齐仓储级回归与 snapshot refresh 回归 | 继续收口整改清单中 P3“约束、软关联与一致性检查”，让 snapshot 状态和整机关联关系在仓储层保持一致，减少历史脏数据或旁路写入带来的错链风险 | `pytest tests/test_repository/test_order_schedule_snapshot_repo.py tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 20 passed |
| 2026-03-27 | snapshot 模型层状态-整机关联守护下沉 | 在 `models/order_schedule_snapshot.py` 中新增状态-整机关联 validator，直接构造 `OrderScheduleSnapshot(...)` 时也会自动清空非 `scheduled / scheduled_stale` 状态下的 `machine_schedule_id`；同步补齐模型级与仓储级回归 | 继续收口整改清单中 P3“约束、软关联与一致性检查”，把 snapshot 状态-整机关联的一致性守护从仓储层再下沉到模型层，进一步降低旁路写入不一致状态的风险 | `pytest tests/test_repository/test_order_schedule_snapshot_repo.py tests/test_scheduler/test_schedule_snapshot_refresh_service.py -q` 21 passed |
| 2026-03-27 | DataIssue 自然键规则收口 | 在 `data_issue_repo.py` 中把订单型 issue 的 `upsert_open_issue(...)` 匹配条件收口为 `issue_type + source_system + order_line_id + open`，不再把 `issue_title` 文案纳入订单型去重条件；标题变化时会更新现有 issue，而不是新插一条 open issue | 继续收口整改清单中 P3“约束、软关联与一致性检查”，降低 issue 标题文案调整导致的重复 open issue 风险，同时保持非订单型 `biz_key` issue 的现有行为不变 | `pytest tests/test_repository/test_data_issue_repo.py tests/test_api/test_issues_api.py -q` 10 passed |
| 2026-03-27 | DataIssue 模型层订单键守护下沉 | 在 `models/data_issue.py` 中新增 `biz_key -> order_line_id` validator，并让 `data_issue_repo.py` 复用同一归一逻辑；直接构造 `DataIssueRecord(...)` 时也会自动补齐订单键或拒绝错链，不再只依赖仓储入口兜底 | 继续收口整改清单中 P3“约束、软关联与一致性检查”，把订单型 issue 的一致性守护从仓储层扩展到模型层，进一步降低旁路写入脏数据的风险 | `pytest tests/test_repository/test_data_issue_repo.py tests/test_api/test_issues_api.py -q` 12 passed |
| 2026-03-27 | DataIssue 历史重复 open issue 自动收口 | 在 `data_issue_repo.py` 中把订单型 open issue 的查找改为按自然键选最新一条，并在发现历史重复 open issue 时自动把更早记录标记为 resolved；同步补齐仓储级回归与 issues API 回归 | 继续收口整改清单中 P3“约束、软关联与一致性检查”，让历史脏数据不会长期污染当前 open issue 查询结果，同时不引入新的数据库迁移风险 | `pytest tests/test_repository/test_data_issue_repo.py tests/test_api/test_issues_api.py -q` 13 passed |
| 2026-03-27 | DataIssue source_system 空值归一收口 | 在 `models/data_issue.py` 与 `data_issue_repo.py` 中把 `source_system` 的空值统一归一到 `None`，并补齐仓储级回归与 issues API 回归，确保非订单型 issue 不会因 `None / 空字符串 / 空白` 混用而被误判为不同自然键 | 继续收口整改清单中 P3“约束、软关联与一致性检查”，让 issue 自然键在模型层和仓储层保持一致，减少隐性重复 open issue 风险 | `pytest tests/test_repository/test_data_issue_repo.py tests/test_api/test_issues_api.py -q` 16 passed |


## 当前待优化项（P0~P4）

### P0
1. 当前整改清单既定 P0 已全部收口；后续如出现 PostgreSQL 生产差异，继续按真实环境回归补强。

### P1
1. 持续检查发图、快照、导出链路是否仍有隐藏 N+1。
2. 在现有权限点骨架上继续补权限矩阵与前后端联动说明。
3. 继续拆分超大文件并推进排产语义治理。

### P2
1. 继续补禁用用户、空角色、非 admin、角色切换回归，以及更贴近真实部署的任务恢复集成演练。

### P3
1. 持续拆分超大服务文件，降低维护与回归风险。

### P4
1. 在权限点骨架稳定后，再扩展角色权限分配、中文角色口径与更细粒度 RBAC。

## 接下来的执行步骤
1. 在现有 `require_permission(...)` 骨架与角色权限分配接口上继续推进权限矩阵与前后端联动说明。
2. 继续拆分超大 service / repository 文件，并把 `pending_drawing`、周期、触发日期等语义治理转入“先文档后代码”的复核路径。
3. 把现有恢复/可观测与权限联动能力继续沉淀为可执行的前后端联调说明。
4. 每完成一项整改后同步回写后端 progress 文档。




